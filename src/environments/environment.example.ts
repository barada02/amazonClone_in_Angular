/**
 * IMPORTANT: This is an example environment file.
 * 
 * Instructions for new developers:
 * 1. Copy this file and rename it to 'environment.ts'
 * 2. Update the databaseURL with the correct Firebase Realtime Database URL
 * 3. Make sure not to commit your environment.ts file to the repository
 *    (it should already be in .gitignore)
 */

export const environment = {
  production: false,
  databaseURL: "https://your-firebase-project-id.firebasedatabase.app"
};
